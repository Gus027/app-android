import { SafeAreaView, Text } from "react-native/types";

const App = () => {
    return(
        <SafeAreaView>
            <Text>Hello Word</Text>
        </SafeAreaView>
    );
}

export default App;